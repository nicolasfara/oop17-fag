package it.unibo.goffo.fag.spawner.controller;

/**
 * Enum to define which spawning mode use the SpawnController.
 */
public enum SpawnMode {
    /**
     * Define a linear spawning type on time.
     */
    LINEAR,
    /**
     * Define an exponential spawning type on time.
     */
    EXP;
}
